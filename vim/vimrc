"
" Settings
"

so $HOME/.vim/settings/general.vim       " mouse, leader, etc.
so $HOME/.vim/settings/plugins.vim       " plugins and their configurations
so $HOME/.vim/settings/appearance.vim    " theme
so $HOME/.vim/settings/editing.vim       " text editing
so $HOME/.vim/settings/bindings.vim      " quick access to navigation, editing
